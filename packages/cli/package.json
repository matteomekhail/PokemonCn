{
  "name": "pokemoncn",
  "version": "0.0.1",
  "description": "PokemonCn CLI — thin wrapper around `shadcn` that points at https://pokemoncn.dev.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "pokemoncn": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "execa": "^9.5.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsup": "^8.3.5",
    "typescript": "^5.6.3"
  }
}
